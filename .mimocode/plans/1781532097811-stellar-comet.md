# Fix Plan: B-Roll Studio Bug Fixes

## Overview
Fix actual bugs, broken code, memory leaks, and code quality issues. Leave license bypass as-is per user request.

---

## Step 1: `lib/constants.js` — Add missing MSG constants

Add these message types that are currently used as inline strings:
- `REDRAW_SCENE_RECTS`
- `HIDE_OVERLAY`
- `SHOW_OVERLAY`
- `CANCEL_RENDER`
- `PING`

**File:** `lib/constants.js` (add to MSG object)

---

## Step 2: `offscreen/offscreen.js` — Fix duplicate declarations + MessageChannel leak

**2a. Remove duplicate `useWatermark`/`renderBrandKit` (lines 125-130)**
- Move the declarations before the `if (isVideo)` block so both the first-frame pre-render and the main loop use the same variables
- Delete lines 125-130 (the duplicate inside the `if` block)
- Keep lines 146-150 (the function-level declarations)

**2b. Fix `frameDelay` MessageChannel port leak (lines 252-265)**
- Close `ch.port1` and `ch.port2` when the promise resolves

---

## Step 3: `lib/export.js` — Fix GIF encoder + downloadBlob

**3a. Rewrite `lzwEncode` with proper variable-width bit packing (lines 230-258)**
- Implement proper LZW with bit buffer that accumulates bits and flushes complete bytes
- Start at 9-bit codes, grow to 12-bit, emit clear code when table is full
- Current implementation writes raw bytes without bit packing — produces corrupt GIFs

**3b. Fix `downloadBlob` synchronous URL revoke (lines 43-50)**
- Use `setTimeout(() => URL.revokeObjectURL(url), 1000)` instead of synchronous revoke after `a.click()`

---

## Step 4: `lib/capture.js` — Close ImageBitmap objects

- After drawing each bitmap to canvas (line 24), call `bitmap.close()`
- After creating the final stitched bitmap (line 28), ensure the caller closes it (the offscreen.js caller should close `imageBitmap` after render completes)

---

## Step 5: `background/service-worker.js` — Multiple fixes

**5a. Remove unused imports (line 2)**
- Remove `getLicense` and `saveLicense` from the import

**5b. Add null check on `win` (line 181)**
- `const win = await chrome.tabs.get(targetTabId)` — add null check before accessing `win.windowId`

**5c. Remove duplicate `calculateStripOffsets` (lines 73-86)**
- Import from `../lib/capture.js` instead of defining locally

**5d. Replace hardcoded message strings with MSG constants**
- `'REDRAW_SCENE_RECTS'` → `MSG.REDRAW_SCENE_RECTS` (lines 27, 49, 405)
- `'HIDE_OVERLAY'` → `MSG.HIDE_OVERLAY` (line 155)
- `'SHOW_OVERLAY'` → `MSG.SHOW_OVERLAY` (line 209)
- `'CANCEL_RENDER'` → `MSG.CANCEL_RENDER` (lines 390, 394)
- `'PING'` → `MSG.PING` (line 105)

**5e. Fix download listener leak (lines 358-369)**
- Add cleanup for cases where download is cancelled externally (use a timeout fallback)

---

## Step 6: `cloudflare-worker/worker.js` — Fix key parsing

**6a. Fix `key.split('-')` to handle base64url with `-` (line 53)**
- Use `key.indexOf('-')` for first split and `key.lastIndexOf('-')` for last split
- Extract prefix, payload, and sig correctly

**6b. Replace `atob()` with base64url decode (line 65)**
- Replace `-` with `+` and `_` with `/` before calling `atob()`

**6c. Use constant-time signature comparison (lines 97-100)**
- Use `crypto.subtle.timingSafeEqual()` for HMAC comparison

---

## Step 7: Clean up unused imports

| File | Remove |
|------|--------|
| `lib/canvas-compositor.js:1` | `lerp`, `clamp` |
| `lib/presets.js:1` | `DEFAULTS` |
| `sidepanel/sidepanel.js:5,7` | `getExportCount`, `deleteProject`, `isFreeUser`, `canExport`, `getRemainingFreeExports` |

---

## Step 8: Replace hardcoded strings in `sidepanel/sidepanel.js` and `content/content.js`

**sidepanel/sidepanel.js** (ES module — can import MSG):
- `'REDRAW_SCENE_RECTS'` → `MSG.REDRAW_SCENE_RECTS`
- `'CANCEL_RENDER'` → `MSG.CANCEL_RENDER`

**content/content.js** (IIFE — cannot import):
- Add hardcoded MSG values as local constants at top of IIFE, OR
- Leave as-is (these are stable message strings unlikely to change)
- Also fix `return true` → only return true when `sendResponse` was actually called (for `GET_PAGE_DIMENSIONS` and `SCROLL_TAB` cases)

---

## Step 9: Close ImageBitmap in offscreen.js after render

In the `finally` block of `handleRenderVideo`, close the `imageBitmap` created at line 108 to prevent GPU memory leaks.

---

## Verification

1. Load the unpacked extension in Chrome (`chrome://extensions` → Load unpacked)
2. Test text selection on a webpage → verify overlay appears
3. Test export (MP4/WebM) → verify video renders without errors in console
4. Test GIF export → verify the GIF file is valid and displays correctly
5. Test PNG sequence export → verify ZIP downloads
6. Test cancel render → verify no console errors
7. Check Chrome DevTools console for any runtime errors
8. Test license activation flow → verify it reaches the worker (even though URL is placeholder)
